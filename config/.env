# DEPRECATED: Use root .env file instead
#
# This file has been consolidated into the root .env file.
# All environment variables should now be set in: ./.env
#
# Historical content (for reference only - DO NOT USE):
# This file was used for GitHub Actions secrets (CI/CD pipeline)
# Format: CLAUDE_API_KEY=${{ secrets.CLAUDE_API_KEY }}
#
# For local development, use: ./.env (see .env.example for template)
# For GitHub Actions, set secrets in: Settings → Secrets and variables → Actions
#
# Consolidation date: 2026-06-16
