# Pipelines, Validation, and Recovery

Use:
```bash
socrata pipeline ... --required-col <col> --state-path outputs/pipeline_state.json --report-path outputs/pipeline_run_report.json
```

- `required-col`: strict validation gate
- `state-path`: resume context
- `report-path`: run output metadata
